# Poco lib: built via Conan (conanfile.py). Run: python scripts/build.py <platform>
cmake_minimum_required(VERSION 3.20)
project(poco-lib VERSION 1.14.2 LANGUAGES C CXX)

message(STATUS "poco-lib: use prebuilt from output/ or build via Conan: python scripts/build.py <platform>")
